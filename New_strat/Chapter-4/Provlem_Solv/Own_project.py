# # ok lets try to make a calendar

# # we need to import a module call calender
#  #so ipmort calender
# import calendar

# # Now we have to take Year input
# year = int(input("Enter the year: "))

# # And now we have to take month input
# month = int(input("Enter the month: "))

# # And now we just print the calendar
# print(calendar.month(year, month))

# now A clear code

import calendar

Year = int(input("Enter the year: "))
Month = int(input("Enter the month: "))

print(calendar.month(Year, Month))