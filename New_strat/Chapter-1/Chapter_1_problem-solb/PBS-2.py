# this Program code is use for make a list and print the dairectory using the os modiulA is use for make a list and print the dairectory using the os modiul

import os
#this code select the directory path
directory_path = '/'
#this code list the contents of the directory
contents = os.listdir(directory_path)
# this code prints the contents of the directory
print(contents)
